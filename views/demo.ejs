<head>   
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="stylesheet" href="/css/styleStudy.css">
    <meta name="viewport" content= "width=device-width, initial-scale=1.0">
	
</head>
<Header class="notFlex">
    <div class="demoHeader">
        <div>
            <span id="demo" class="demoHeader2">Demo Language:</span>
            <span id="language" class="demoHeader2"> <%= language %></span>
        </div>
        <div>
            <span class="demoHeader2">Activity:</span>
            <span id="activity" class="demoHeader2"><%= activity %></span>
        </div>
         
    </div>
</Header>
<body class="coolBackground">

<section class="vocabContainer" id="end">
    <% for(let i=0; i < vocabList.length; i++) {%> 
        <div id="A<%= 1 + i %>" style="background-image: url('<%= vocabList[i].imageUrl %>')">
            <audio src="<%= vocabList[i].audioQ %>" id="audioQ<%= 1 + i %>"></audio>
            <audio src="<%= vocabList[i].audioN %>" id="audioN<%= 1 + i %>"></audio>
            <audio src="<%= vocabList[i].audioTis %>" id="audioTis<%= 1 + i %>"></audio> 
        </div>
    <% } %>

       

</section>

<div class="controls controlContainer">
    <div class="Controls" id="demoControls"> 
        <button class="control" id="review" type="button" disabled>Review</button>
        <button class="control" id="start" type="button" disabled>Learn</button>
        <button class="control" popovertarget="dialogPop" id="help" type="button">Help</button>
    </div>
    <div class="controls">
        <button class="control" id="repeatThat" type="button" disabled>Repeat the Question</button>
        <button class="control" id="undo" type="button" disabled>Remove Last Error</button>
    </div>
    <div class="controls">
        <button class="contorl" id="showAll" type="button" disabled>Show All</button>
        <button class="control" id="select" type="button" disabled>Select Vocab Word</button>
        <button class="control" id="addSelection" type="button" disabled>Add Selected Words</button>
    </div>
</div>   
<dialog id="dialogPop" popover>
    <h1>Welcome to the Advantage demo area.</h1>
    <p>There are two options available in this demo. If you click the "Learn" button it will simulate what would happen in an in person Rough and Ready style activity. While if you click the "Review" button you will review the activity like you would at the end of a Rough and Ready style activity.</p>
</dialog>

<div class="navFoot">
    <button type="button" class='navButton' id='back'>Back to Login</button>
</div>

<script type="text/javascript" src="/js/demo.js"></script>
</body>
